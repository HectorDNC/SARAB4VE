PORT=3001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sara

# Cloudflare R2 (almacenamiento de audio para reportes por voz)
R2_ENDPOINT=https://<tu-account-id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=<tu-access-key-id>
R2_SECRET_ACCESS_KEY=<tu-secret-access-key>
R2_BUCKET=sara-audio
R2_PUBLIC_URL=https://pub-<hash>.r2.dev

# Google AI Studio / Gemini — Extracción de emergencia vía LLM
# Obtén tu API key gratis en https://aistudio.google.com/app/apikey
# El sistema intenta Gemini primero; si falla, usa Groq; si tampoco, usa fallback por diccionario.
GEMINI_API_KEY=
# Modelo a usar (opcional, default: gemini-1.5-flash — rápido y económico para extracción estructurada)
GEMINI_MODEL=gemini-1.5-flash

# Groq — Fallback de extracción de emergencia vía LLM gratuito
# Obtén tu API key gratis en https://console.groq.com
# El sistema funciona igual sin ella (usa fallback por diccionario de palabras clave)
GROQ_API_KEY=
# Modelo a usar (opcional, default: llama-3.3-70b-versatile)
GROQ_MODEL=llama-3.3-70b-versatile
